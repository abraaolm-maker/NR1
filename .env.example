# Copiar para .env.local e preencher. .env.local nunca deve ser commitado.

DJANGO_SECRET_KEY=troque-por-uma-chave-secreta-gerada
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=

# Banco de dados: SQLite3 definitivo (CLAUDE.md Seção 0 / Seção 4.1 item 5).
# Sem variáveis de conexão a configurar — o arquivo db.sqlite3 vive na raiz do projeto.

# Anthropic (Etapa 5 — API de análise, CLAUDE.md Seção 8). Nunca commitar o valor real;
# preencher só em .env.local.
ANTHROPIC_API_KEY=
